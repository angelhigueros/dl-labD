# Angel Higueros - 20460
# Laboratorio C

import graphviz

def display_expression_tree(root, regex):  # sourcery skip: remove-unnecessary-cast
    # graph = graphviz.Digraph()
    # graph.node("root", label="MEGA AUTOMATA")   
    # graph.attr(label=f'regex: {regex}')

    # def add_node(node):
    #     graph.node(str(id(node)), label=node.value)
    #     if node.left:
    #         add_node(node.left)
    #         graph.edge(str(id(node)), str(id(node.left)))
    #     if node.right:
    #         add_node(node.right)
    #         graph.edge(str(id(node)), str(id(node.right)))

    # add_node(root)
    # graph.edge("root", str(id(root))) 
    # graph.render("expression_tree", format="png", view=True) 
    pass